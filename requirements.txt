# Slim dependencies for Vercel serverless deployment.
# For local dev with all features (CLI, ML, Gmail API), use:
#   pip install -r requirements-full.txt

python-dotenv>=1.0.0
jinja2>=3.1.0
pyyaml>=6.0.0
flask>=3.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# Persistent cloud storage on Vercel (Turso / libsql). Linux wheels install
# cleanly on Vercel; only the local Windows dev install skips it.
libsql-experimental>=0.0.37
